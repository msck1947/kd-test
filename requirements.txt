# ============================================================================
# Knowledge Distillation Framework - Requirements
# ============================================================================
# Core dependencies for the Llama 3.2 Knowledge Distillation Framework

# DistillKit - Knowledge Distillation Toolkit
# Install from source for latest features:
# pip install git+https://github.com/arcee-ai/DistillKit.git
distillkit @ git+https://github.com/arcee-ai/DistillKit.git

# Core ML/DL frameworks
torch>=2.0.0
transformers>=4.45.0
datasets>=2.14.0
accelerate>=0.24.0
trl>=0.7.0

# Training utilities
peft>=0.6.0
bitsandbytes>=0.41.0
safetensors>=0.4.0

# Flash Attention (optional but recommended for speed)
# flash-attn>=2.3.0  # Uncomment if CUDA available and flash-attn can be compiled

# Logging and monitoring
tensorboard>=2.14.0
wandb>=0.15.0

# Configuration and utilities
pyyaml>=6.0
omegaconf>=2.3.0

# Evaluation
evaluate>=0.4.0
scikit-learn>=1.3.0
rouge-score>=0.1.2
bert-score>=0.3.13

# Data processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# HuggingFace Hub
huggingface-hub>=0.17.0

# Tokenizers
tokenizers>=0.14.0
sentencepiece>=0.1.99

# Memory optimization
deepspeed>=0.12.0  # Optional for distributed training

# Utilities
packaging>=23.0
filelock>=3.12.0
requests>=2.31.0